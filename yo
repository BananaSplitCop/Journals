se the xxd filename.img |grep JFIF and find the 1st number that comes up on the left after you rn the command
Then do xxd filename.img |grep ffd9|grep -v "0000 0000" and find the first number in the that line which should be located in the same place as the first command
After that run the command echo "ibase "first # - 2nd #"|bc
Then take that number and put it into the skip place in this command
dd if=Filename.img skip=final # bs=1 count=33616 of=filename.img
Now you have a new recovered disk image





